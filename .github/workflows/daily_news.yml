name: Daily News Digest

on:
  schedule:
    # 3時間おきに更新 (1:00, 4:00, 7:00, 10:00, 13:00, 16:00, 19:00, 22:00 UTC)
    # JST換算: 10:00, 13:00, 16:00, 19:00, 22:00, 翌1:00, 翌4:00, 翌7:00
    - cron: "0 */3 * * *"
  workflow_dispatch:  # 手動実行も可能

permissions:
  contents: write  # GitHub Pagesへのpushに必要

jobs:
  generate:
    runs-on: ubuntu-latest

    steps:
      - name: リポジトリをチェックアウト
        uses: actions/checkout@v4

      - name: Python をセットアップ
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: 依存パッケージをインストール
        run: pip install -r requirements.txt

      - name: ニュースを生成
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: python scripts/generate_news.py

      - name: GitHub Pages へコミット & プッシュ
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add docs/index.html
          git diff --cached --quiet || git commit -m "chore: update news digest $(date +'%Y-%m-%d %H:%M JST' -d '+9 hours')"
          git push
